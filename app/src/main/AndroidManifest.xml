<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naseem.priceomania1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".CardDetails"
            android:label="@string/title_activity_card_details"
            android:theme="@style/AppTheme" />
        <activity android:name=".BottomBar.Activity1" />
        <activity android:name=".BottomBar.Activity2" />
        <activity android:name=".BottomBar.Activity3" />
        <activity android:name=".BottomBar.Activity4" />
        <activity android:name=".ExpandableActivity" />
        <activity android:name=".DetailsActivityfldr.DetailsActivity" />
        <activity
            android:name=".DialogActivity"
            android:parentActivityName=".CardDetails" />
        <activity
            android:name=".SeeAllOptions"
            android:parentActivityName=".DialogActivity" />
        <activity
            android:name=".ColorActivity"
            android:parentActivityName=".DialogActivity" />
        <activity
            android:name=".StorageActivity"
            android:parentActivityName=".DialogActivity" />
        <activity
            android:name=".NetworkActivity"
            android:parentActivityName=".DialogActivity" />
        <activity android:name=".MoreSites.MoreSitesActivity" />
    </application>

</manifest>